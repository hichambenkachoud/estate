{% extends 'base-back.html.twig' %}

{% block title %}
    {{ 'admin.agency.management' | trans }}
{% endblock %}

{% block body %}
    <header class="page-header">
        <h2>{{ 'admin.agency.management' | trans }}</h2>

        <div class="right-wrapper text-right">
            <ol class="breadcrumbs">
                <li>
                    <span><i class="fas fa-tasks"></i></span>
                </li>
                <li><span>{{ 'admin.agency' | trans }}</span></li>
                <li><span>{{ 'admin.list' | trans }}</span></li>
            </ol>

            <a class="sidebar-right-toggle" href="#"></a>
        </div>
    </header>

    <div class="row">
        <div class="col">
            <section class="card card-warning">
                <header class="card-header">
                    <h2 class="card-title"></h2>
                </header>
                <div class="card-body">
                    <div class="col-sm-6">
                        <div class="mb-3">
                            <a class="btn btn-primary btn-sm" href="{{ path('agency_new') }}">{{ 'admin.add'|trans }} <i class="fas fa-plus"></i></a>
                        </div>
                    </div>
                    <table class="table table-bordered table-striped mb-0" id="datatable-default">
                        <thead>
                        <tr>
                            <th>{{ 'admin.code' | trans }}</th>
                            <th>{{ 'admin.name' | trans }}</th>
                            <th>{{ 'admin.mobileNumber' | trans }}</th>
                            <th>{{ 'admin.create_date' | trans }}</th>
                            <th>{{ 'admin.enabled' | trans }}</th>
                            <th>{{ 'admin.actions' | trans }}</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for agency in agencies %}
                            <tr>
                                <td>{{ agency.code }}</td>
                                <td>{{ agency.name }}</td>
                                <td>{{ agency.mobileNumber }}</td>
                                <td>{{ agency.createDate ? agency.createDate|date('Y-m-d H:i:s') : '' }}</td>
                                <td>{{ agency.enabled ? 'Yes' : 'No' }}</td>
                                <td>
                                    <a href="{{ path('agency_show', {'id': agency.id}) }}"><i class="fas fa-eye"></i></a>
                                    <a href="{{ path('agency_edit', {'id': agency.id}) }}"><i class="fas fa-pencil-alt"></i></a>
                                </td>
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="7">{{ 'admin.no_records_found' | trans }}</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </section>
        </div>
    </div>
{% endblock %}
