<form method="post" action="{{ path('country_delete', {'id': country.id}) }}" onsubmit="return confirm('{{ 'admin.delete.sure' | trans }}');" style="display: inline;">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ country.id) }}">
    <button class="btn btn-danger btn-sm">{{ 'admin.delete' |trans }}</button>
</form>
